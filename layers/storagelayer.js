const saveLoginData = (username) => {
    // Logic for saving login data
    console.log(`Saving login data for ${username}`);
};

const saveFormData = (name, email) => {
    // Logic for saving form data
    console.log(`Saving form data for ${name}, ${email}`);
};

module.exports = {
    saveLoginData,
    saveFormData,
};
